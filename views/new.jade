extends layout

block content
    h1= title
    form#formAddUser(name="add",method="post",action="/add")
        input#inputUserName(type="text", placeholder="pin", name="pin")
        input#inputUserEmail(type="text", placeholder="email", name="email")
        button#btnSubmit(type="submit") submit